[project]
name = "rememberme"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "apscheduler>=3.11.3",
    "pillow>=12.3.0",
    "plyer>=2.1.0",
    "pystray>=0.19.5",
    "typer>=0.27.2",
]

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
